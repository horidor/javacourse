public class CommuterPlane extends Plane {
    public CommuterPlane(int id, String model, float flightDistance, float weightCapacity, int passengerCapacity, float fuelConsumptionPerKm) {
        super(id, model, flightDistance, weightCapacity, passengerCapacity, fuelConsumptionPerKm);
    }
}
